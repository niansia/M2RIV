{
  "$defs": {
    "ConformanceProfile": {
      "additionalProperties": false,
      "description": "One normative evaluation-decision interoperability observation.",
      "properties": {
        "decision_status": {
          "$ref": "#/$defs/MCRStatus"
        },
        "evaluation_policy_satisfied": {
          "title": "Evaluation Policy Satisfied",
          "type": "boolean"
        },
        "evidence_id": {
          "pattern": "^mcr:sha256:[a-f0-9]{64}$",
          "title": "Evidence Id",
          "type": "string"
        },
        "profile": {
          "enum": [
            "pass",
            "warn",
            "insufficient_power",
            "block",
            "error"
          ],
          "title": "Profile",
          "type": "string"
        },
        "report_id": {
          "pattern": "^mcr:sha256:[a-f0-9]{64}$",
          "title": "Report Id",
          "type": "string"
        }
      },
      "required": [
        "profile",
        "report_id",
        "evidence_id",
        "decision_status",
        "evaluation_policy_satisfied"
      ],
      "title": "ConformanceProfile",
      "type": "object"
    },
    "MCRStatus": {
      "enum": [
        "PASS",
        "WARN",
        "INSUFFICIENT_POWER",
        "BLOCK",
        "ERROR"
      ],
      "title": "MCRStatus",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Machine-readable outcome for one producer or consumer conformance run.",
  "properties": {
    "checks": {
      "items": {
        "type": "string"
      },
      "maxItems": 64,
      "minItems": 1,
      "title": "Checks",
      "type": "array"
    },
    "conformant": {
      "const": true,
      "default": true,
      "title": "Conformant",
      "type": "boolean"
    },
    "id": {
      "pattern": "^mcr:sha256:[a-f0-9]{64}$",
      "title": "Id",
      "type": "string"
    },
    "implementation_name": {
      "anyOf": [
        {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Implementation Name"
    },
    "implementation_version": {
      "anyOf": [
        {
          "pattern": "^[A-Za-z0-9][A-Za-z0-9.+_-]{0,63}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Implementation Version"
    },
    "profiles": {
      "items": {
        "$ref": "#/$defs/ConformanceProfile"
      },
      "maxItems": 5,
      "minItems": 5,
      "title": "Profiles",
      "type": "array"
    },
    "schema_version": {
      "const": "0.3.0",
      "default": "0.3.0",
      "title": "Schema Version",
      "type": "string"
    },
    "subject": {
      "enum": [
        "producer",
        "consumer"
      ],
      "title": "Subject",
      "type": "string"
    },
    "warnings": {
      "default": [],
      "items": {
        "type": "string"
      },
      "maxItems": 64,
      "title": "Warnings",
      "type": "array"
    }
  },
  "required": [
    "id",
    "subject",
    "profiles",
    "checks"
  ],
  "title": "MCRConformanceResult",
  "type": "object"
}
